{
  "type": "module",
  "private": true,
  "workspaces": [
    "server",
    "client",
    "e2e"
  ],
  "scripts": {
    "build:client": "yarn workspace restless-redhead-client build",
    "dev": "yarn workspace restless-redhead-server dev",
    "dev:debug": "yarn workspace restless-redhead-server dev:debug",
    "heroku-postbuild": "yarn workspace restless-redhead-client build",
    "start": "yarn workspace restless-redhead-server start",
    "dev:cypress": "yarn workspace restless-redhead-server dev:e2e",
    "e2e:open": "yarn workspace restless-redhead-e2e e2e:open",
    "e2e:run": "yarn workspace restless-redhead-e2e e2e:run"
  },
  "engines": {
    "node": "^14.15",
    "yarn": "1.22.10"
  }
}
